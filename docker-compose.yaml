# この中でDockerfileを呼び出してDockerコンテナのビルドを行う

version: '3'
services:
  demo-app:
    build: .
    volumes:
      - .dockervenv:/src/.venv
      - .:/src
    ports:
      - 8000:8000 #ホストマシンのポート8000をdocker内のポート8000に接続
